[package]
name = "dirty"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
git2 = "0.20"
libc = "0.2"
rayon = "1"

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = true
strip = true
